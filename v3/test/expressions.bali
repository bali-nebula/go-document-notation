{
    assign $foo := ~π(
        $units: $radians
    )
    assign $foo := sum(
        a
        b
    )
    assign $foo := range([0..))
    assign $foo := foo<-bar(x)
    assign $foo := |z|
    assign $foo := |3-4i|
    assign $foo := |"Hello, 世界"|
    assign $foo := -/*z
    assign $foo := x + 1
    assign $foo := x + y
    assign $foo := x + y + z
    assign $foo := (x + y)
    assign $foo := (x + y - z) * (x / y % z)
    assign $foo := base ^ phase-angle
    assign $foo := pin
    assign $foo := φ + π
    assign $foo := not p
    assign $foo := p and q
    assign $foo := not (p and q) ior (p ior not q) xor (p san q)
    assign $foo := @reference
    assign $foo := list<-add-value(ex)
    assign $foo := remote<~notify(event)
    assign $foo := customer[42 $address "violet"]
    assign $foo := a & b & c
    assign $foo := a < b and b = c san d > e ior e is f xor f matches g
}
